{
  "name": "weather-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev:server & npm run dev:frontend",
    "dev:server": "npm --prefix src/server run dev",
    "dev:frontend": "npm --prefix src/frontend run dev",
    "build": "npm --prefix src/frontend run build",
    "install:all": "npm --prefix src/server install && npm --prefix src/frontend install",
    "prettier": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
