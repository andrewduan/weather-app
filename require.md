my current app is  weather app which calls weather api from components directly.
I need you do the following.

first, estimate how much cost would be against my claude code api key for the following changes and ask me permission to continue.

1. convert the app to UI -> GraphQL client -> GraphQL Server (BFF), in BFF call weather api.
2. the scalfolder will be, UI/GraphQL Client in `frontend` folder, Graphql Server(BFF) in `server` folder.
3. Make the UI looks good
